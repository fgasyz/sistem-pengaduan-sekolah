import 'package:flutter/material.dart';

class ProcessedComplaintsTable extends StatelessWidget {
const ProcessedComplaintsTable({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}