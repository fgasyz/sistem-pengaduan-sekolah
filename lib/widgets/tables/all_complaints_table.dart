import 'package:flutter/material.dart';

class AllComplaintsTable extends StatelessWidget {
  const AllComplaintsTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
