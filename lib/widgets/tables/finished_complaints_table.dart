import 'package:flutter/material.dart';

class FinishedComplaintsTable extends StatelessWidget {
const FinishedComplaintsTable({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}