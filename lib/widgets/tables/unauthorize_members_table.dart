import 'package:flutter/material.dart';

class UnauthorizeMembersTable extends StatelessWidget {
const UnauthorizeMembersTable({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}