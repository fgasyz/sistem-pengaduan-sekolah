import 'package:flutter/material.dart';

class IncomingComplaintsTable extends StatelessWidget {
const IncomingComplaintsTable({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}